// The shapes and the encoder live in the viewer now (the offline demo needs a
// real mesh asset too), so this is just the handle the tests import.
export {
  buildMeshAsset, encodeSubmesh, boxMesh, pyramidMesh,
  houseMeshAsset, boxMeshAsset, twoMaterialAsset,
} from "../mesh-encode.js";
